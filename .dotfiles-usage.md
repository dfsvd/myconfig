# Dotfiles 使用手册

## 原理

bare git repo 方式，仓库在 `~/.dotfiles`，工作区就是 `~`。git 状态对普通 `git` 命令不可见，必须通过 `dotfiles` 别名操作。

## 别名

| Shell  | 配置方式                              |
|--------|---------------------------------------|
| zsh    | `~/.zshrc` 已写入 `alias dotfiles=...` |
| fish   | 手动加到 `~/.config/fish/config.fish`：`abbr -a dotfiles 'git --git-dir=$HOME/.dotfiles --work-tree=$HOME'` |

## 常用操作

```bash
# 查看状态（只显示已跟踪文件的变化）
dotfiles status

# 添加新配置文件到版本控制
dotfiles add .config/某配置文件

# 提交修改
dotfiles commit -m "修改说明"

# 查看提交历史
dotfiles log --oneline

# 查看某文件的改动
dotfiles diff .zshrc

# 恢复到上次提交
dotfiles checkout -- .zshrc
```

## 推送到 GitHub

```bash
# 1. 先在 GitHub 创建私有仓库，例如 LiuXin/dotfiles

# 2. 添加远程并推送
dotfiles remote add origin git@github.com:LiuXin/dotfiles.git
dotfiles push -u origin master
```

## 在新机器上恢复

```bash
# 1. 克隆仓库
git clone --bare git@github.com:LiuXin/dotfiles.git $HOME/.dotfiles

# 2. 定义别名
alias dotfiles='git --git-dir=$HOME/.dotfiles --work-tree=$HOME'

# 3. 检出文件
dotfiles checkout

# 4. （可选）隐藏未跟踪文件
dotfiles config status.showUntrackedFiles no
```

## 当前已跟踪文件

- `~/.zshrc`
- `~/.config/fish/config.fish`
- `~/.config/niri/config.kdl`
- `~/.config/starship.toml`
- `~/.config/kitty/kitty.conf`
- `~/.config/nvim/`（全部）
